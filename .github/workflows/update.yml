name: Auto Update Content on: schedule: - cron: '0 */6 * * *' workflow_dispatch: jobs: update: runs-on: ubuntu-latest steps: - uses: actions/checkout@v3 - name: Update content run: | echo "Auto-update: $(date)" >> updates.log git config user.name "Auto Bot" git config user.email "bot@example.com" git add updates.log git commit -m "Auto-update $(date)" || echo "No changes" git push
